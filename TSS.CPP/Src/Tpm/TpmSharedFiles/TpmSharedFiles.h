#pragma once

#include <string>
#include <sstream>
#include <iomanip>
#include <thread>
#include <atomic>

class CTpmSharedFiles
{

public:
    virtual ~CTpmSharedFiles();
             CTpmSharedFiles();

protected:

private:
};